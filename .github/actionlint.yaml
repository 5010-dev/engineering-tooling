paths:
  .github/workflows/golden-path-quality.yml:
    ignore:
      # GitHub.com exposes these called-workflow identity fields, but
      # actionlint 1.7.12 has not added them to its job-context model yet.
      - 'property "workflow_(ref|repository|sha)" is not defined in object type'
