[[if .NodeExecutable]]import { pathToFileURL } from "node:url";

[[end]][[if .AWSCDK]]import { App, Stack } from "aws-cdk-lib";

const app = new App();
new Stack(app, "[[.StackName]]");
[[else if .Pulumi]]import * as pulumi from "@pulumi/pulumi";

export const pulumiProject = pulumi.getProject();
[[end]]export function projectName(): string {
  return [[.ProjectNameTypeScriptQuoted]];
}[[if .NodeExecutable]]

export function main(): void {
  process.stdout.write(`${projectName()}\n`);
}

if (process.argv[1] !== undefined && import.meta.url === pathToFileURL(process.argv[1]).href) {
  main();
}[[end]]
