import { describe, expect, it } from "vitest";

import { projectName } from "../src/index.js";

describe("projectName", () => {
  it("returns the generated project name", () => {
    expect(projectName()).toBe([[.ProjectNameTypeScriptQuoted]]);
  });
});
