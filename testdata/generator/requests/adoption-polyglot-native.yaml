schemaVersion: golden-path-generator-request/v1
materializationMode: adoption
layout: polyglot
projectName: Existing Polyglot Native Repository
projectSlug: existing-polyglot-native-repository
targets:
  - os: linux
    architecture: amd64
    runtime: container
    tier: primary
    execution: true
components:
  - name: liveness-ticker
    path: crates/liveness-ticker
    profiles: [python, rust]
    artifactTypes: [library, package]
    capabilities: [build, native-extension, package, test]
  - name: adapters
    path: packages/adapters
    profiles: [python, rust]
    artifactTypes: [library, package]
    capabilities: [build, native-extension, package, test]
  - name: web
    path: apps/web
    profiles: [node-typescript]
    artifactTypes: [application]
    capabilities: [build, test, typecheck]
