version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  default: none
  enable:
    - errorlint
    - errcheck
    - gosec
    - ineffassign
    - misspell
    - nolintlint
    - staticcheck
    - unused

formatters:
  enable:
    - gofmt
    - goimports
