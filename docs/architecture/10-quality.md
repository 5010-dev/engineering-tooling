# 10. Quality

State: **As-built**

Unit and integration tests cover authority parsing and hostname pinning, selected normative/profile manifests and post-plan authority drift, credential redaction across URL user information, query, fragment, and SCP-like origins, dual-host idempotent installation, unmanaged, modified, and symlink-redirected Skill protection, non-destructive pnpm cache ownership, read-only planning, exact-plan approval binding, exact apply, overwrite and rollback mode preservation, branch and content drift, case-folded Git metadata, case-aliased and Unicode-normalized destination collisions, symlink traversal, external-plan placement, pnpm workspace-member and nested-fixture ownership, order-independent member and descendant exclusions, excluded and member-lock roots, manifestless workspace roots, disabled shared-lock mode, complete nested workspaces, nested Node/Go/Python and explicit polyglot root classification, and complete report-only evidence identity.

Canonical `just ci` runs format, typed lint, TypeScript analysis, tests, arc42 structure and link checks, build, exact tarball allowlist inspection, publint, Are the Types Wrong, packed CLI and dual-host Skill installation, and an isolated ESM consumer. A release job retains and publishes that same validated tarball. GitHub CI additionally smokes the public entry point on the minimum supported Node line.

These checks prove local and hosted package quality only. They do not prove package publication, fresh-session Skill activation, consumer adoption, deployment, security closure, or platform enforcement.
