# 2. Constraints

State: **As-built**

- Active policy is reviewed source on `5010-dev/.github/main`; this package contains no normative snapshot.
- The package is private `@5010-dev/golden-path-agent` and does not reuse the retired `golden-path` executable identity.
- Historical repository tags and public releases `v0.1.0` through `v1.6.1` are immutable audit history.
- Consumer manifests, locks, native roots, Just graph, CI, security, releases, and deployments remain repository-owned.
- The Skill is developer-installed and explicit-only for both supported hosts.
- Pull requests and `dev` do not publish. Publication requires an immutable package-specific tag at validated `main`.
